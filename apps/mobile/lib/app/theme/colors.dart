import 'package:flutter/material.dart';

abstract final class ClimbrColors {
  // ── Brand ───────────────────────────────────────────────────────────────────
  static const brandCyan       = Color(0xFF0CC0DF);
  static const brandCyanDark   = Color(0xFF09A6C2);
  static const brandCyanSoft   = Color(0xFFE6F9FC);
  static const brandPink       = Color(0xFFEC4899);
  static const brandOrange     = Color(0xFFFF8A3D);
  static const brandOrangeSoft = Color(0xFFFFE8D6);
  static const brandYellow     = Color(0xFFFFC93C);
  static const brandHotPink    = Color(0xFFFF3D7F);
  static const brandNavy       = Color(0xFF0F1A1F);

  // ── Light neutrals ──────────────────────────────────────────────────────────
  static const bgPrimary     = Color(0xFFFFFFFF);
  static const bgSecondary   = Color(0xFFF7F8FA);
  static const bgTertiary    = Color(0xFFF1F3F5);
  static const border        = Color(0xFFE5E7EB);
  static const borderStrong  = Color(0xFFD1D5DB);
  static const textPrimary   = Color(0xFF0F1A1F);
  static const textSecondary = Color(0xFF6B7280);
  static const textTertiary  = Color(0xFF9CA3AF);

  // ── Dark neutrals ───────────────────────────────────────────────────────────
  static const darkBgPrimary     = Color(0xFF0B1014);
  static const darkBgSecondary   = Color(0xFF12181D);
  static const darkBgTertiary    = Color(0xFF1A2128);
  static const darkBorder        = Color(0xFF232C35);
  static const darkTextPrimary   = Color(0xFFF5F7FA);
  static const darkTextSecondary = Color(0xFFA0AAB4);
  static const darkCyanSoft      = Color(0xFF0E2A33);
  static const darkOrangeSoft    = Color(0xFF3D2A18);

  // ── Status ──────────────────────────────────────────────────────────────────
  static const statusInReview       = Color(0xFFFFA63D);
  static const statusInReviewBg     = Color(0xFFFFF1DE);
  static const statusAccepted       = Color(0xFF1FBE6E);
  static const statusAcceptedBg     = Color(0xFFDFF7E9);
  static const statusShortlisted    = Color(0xFF7C5CFF);
  static const statusShortlistedBg  = Color(0xFFEBE5FF);
  static const statusPending        = Color(0xFF0CC0DF);
  static const statusPendingBg      = Color(0xFFE6F9FC);
  static const statusRejected       = Color(0xFFE5484D);
  static const statusRejectedBg     = Color(0xFFFCE4E6);
}
